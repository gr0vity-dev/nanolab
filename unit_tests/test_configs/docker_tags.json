{
    "testcase" : "docker_tags",
    "docker_tags" : ["alpine", "nanocurrency/nano:V24.0"],
    "commands": [
        {
            "type": "bash",
            "command":  "echo {docker_tag}"
        }
    ]
}
